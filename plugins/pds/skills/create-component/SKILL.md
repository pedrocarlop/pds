---
name: create-component
description: Create a new reusable PDS React library component from a user brief, for example "create a banner", or from pasted unstyled React component code. Use when the component should be added to @pds/react, documented with a component contract, styled with PDS tokens, tested, exported, and added to the Ladle component previewer.
allowed-tools: [Bash, Read, Grep, Glob, Edit, MultiEdit, Write]
---

# Create A PDS Component

This is a plugin discovery adapter. Read the canonical workflow at
`../../context/docs/agent/skills/create-component.md` from this file's directory,
then follow it. Keep detailed PDS agent guidance in `docs/agent`, not in this
adapter.
